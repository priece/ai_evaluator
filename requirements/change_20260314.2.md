# change.20260314.2
- 找“AI评委” 相关的UI字符，把所有的“AI评委” 改为 “AI 评委”，AI后面加个半角空格
- 抬头率的值，在 75 ~ 90 之间随机生成，生成后存数据库
- 在座率 放在 “现场氛围” 上面，也就是两者互换一下顺序
- 在座率 的值，在90 ~ 100 之间随机生成，生成后存数据库
- “现场氛围”值的算法，改为，score的值，先去除以10，再取整，得到的结果再×以10，得到的结果再加上一个随机的个位数+随机浮点数，生产后，保留1位小数存数据库
